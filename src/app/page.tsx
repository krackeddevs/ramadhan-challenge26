import { MapClientWrapper } from "@/components/map/map-client-wrapper";

// Fullscreen map homepage — no header/footer scroll layout needed
export default function Home() {
  return <MapClientWrapper />;
}
